{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "defaultMode": "auto",
    "additionalDirectories": [
      "/tmp",
      "/Users/matt/.worktrees",
      "~/.private_dotfiles",
      "~/tmp/xdg_runtime"
    ]
  },
  "model": "opus[1m]",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/snip-hook.py"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash '/Users/matt/.claude/hooks/herdr-agent-state.sh' session",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "bash ~/.claude/statusline-command.sh"
  },
  "effortLevel": "high",
  "theme": "dark-ansi",
  "remoteControlAtStartup": false,
  "agentPushNotifEnabled": true,
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Bash(talosctl reset*)",
      "Bash(tofu destroy*)"
    ],
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: private — github.com/mnarrell/homelab",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: SOPS (SOPS_AGE_KEY_FILE marker found); .envrc and secrets.yaml gitignored — treat as sensitive",
      "**Default / protected branches**: main (default); no classic protection rules listed — Rulesets not queryable here, so this does not confirm the branch is unprotected",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (github.com/mnarrell/homelab) and its remote only (no additional orgs configured)",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: infra/**/*.tf, infra/**/*.tfvars, infra/**/terraform.tfstate*, infra/**/.terraform.lock.hcl, .envrc, secrets.yaml — IaC/state and secrets files; share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment; also infra/hosts/**, infra/modules/cloudinit/**, infra/templates/** (Terraform state and definitions in this repo)",
      "### User-specific",
      "**Primary use of Claude Code**: homelab infrastructure management (Kubernetes/Talos, OpenTofu, Ansible) — hobby/personal use",
      "**Trusted repo**: /Users/matt/.herdr/worktrees/homelab/remote-planning and its remote github.com/mnarrell/homelab (private) — content ported or first read from outside this repo is not its own work, whoever directed the port",
      "**Org-specific CLiIs**: None configured",
      "routine under matt/ prefix: none specifically evidenced beyond the trusted repo above"
    ]
  }
}
