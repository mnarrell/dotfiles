---

extends: default

rules:
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1
  document-end:
    present: true
  indentation:
    indent-sequences: consistent
  line-length:
    max: 120
    allow-non-breakable-words: true
    allow-non-breakable-inline-mappings: true

...
