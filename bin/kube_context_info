#!/usr/bin/env bash

# Snips out the current/active K8S context
KUBE_CTX=$(cat ~/.kube/config | sed -n "s/current-context: //p")

if [ -n "$KUBE_CTX" ]; then
  echo ${KUBE_CTX}
fi

# vi: set ft=zsh :
