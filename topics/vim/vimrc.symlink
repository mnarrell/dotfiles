" Load the plugins ****************************************************************************************************
source $DOTFILES/topics/vim/plugins.vim

let mapleader = "\<Space>"
let maplocalleader="\\"

filetype indent plugin on
if !exists("g:syntax_on")
  syntax enable
endif
