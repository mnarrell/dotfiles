---

output:
  print-issued-lines: false
  # format: line-number

linters-settings:
  govet:
    feildalignment: true
  # golint:
  #   min-confidence: 0
  # gocyclo:
  #   min-complexity: 10
  # maligned:
  #   suggest-new: true
  # dupl:
  #   threshold: 100
  # goconst:
  #   min-len: 2
  #   min-occurrences: 2
  # gofmt:
  #   simplify: true
  misspell:
    locale: US
  lll:
    line-length: 140

linters:
  enable:
    # - gochecknoglobals
    # - gofmt
    # - gomnd
    # - scopelint
    # - staticcheck
    # - wsl
    - bodyclose
    - deadcode
    - depguard
    - dogsled
    - dupl
    - errcheck
    - exportloopref
    - funlen
    - gochecknoinits
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godox
    - goimports
    - goprintffuncname
    - gosec
    - gosimple
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - revive
    - rowserrcheck
    - structcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - varcheck
    - whitespace

issues:
  exclude:
    - Using the variable on range scope .* in function literal

  exclude-rules:
    - linters:
        - staticcheck
      text: "SA3000:"

#     - path: _test\.go
#       linters:
#         - dupl
#         - errcheck
#         - gocyclo
#         - gosec
#         - scopelint

# run:
#   skip-dirs:
#     - testdata

...
