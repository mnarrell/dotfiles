options("repos" = c(CRAN = "https://rweb.quant.ku.edu/cran/"))
options(stringsAsFactors=FALSE)
options(editor="vim")
options(width=200)

library("colorout")

rat <- function() {
  packrat::set_opts(external.packages = "colorout")
  packrat::init()
}
